name: xbot poll
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - name: poll mentions
        run: |
          curl -fsS -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://shit.so/api/cron/xbot
